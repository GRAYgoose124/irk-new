
Bot Commands

send_response(message, original_sender, destination) ->
This function may be invoked by the plugin from it's handler (handler.send_response)
in order to send a private message to a channel or user.


Plugin hooks (Defined in plugin.py)

privmsg_hook(handler, data) ->
This function may be provided by the plugin as a way for the Handler
to invoke the plugin's provided private message features.


Bot built-in commands (Defined in irk.bot.IrcBot#__init__, self.command_dict) ->
These are all the commands provided by the bot itself, they are permanent and always usable.

command_dict {
 `quit`
 `reconnect`
 `join`
 `part`
 `load`
}


Plugin API packet (Defined in irk.client.IrcClient#_process_message, data) ->
This is the format of data that the handler passes the plugin.

data {
  `sender`
  `ident`
  `orig_dest`
  `command`
  `arguments`
}

IRC Client Parameters (Defined in irk.client) ->
Below is both the required parameters as well as some default settings.
required_irc_config = {
    'host': 'irc.foonetic.net', 'port': 7001, 'ipv6': False,
    'nick': '', 'pass': '',
    'ident': '', 'user': '',
    'mode': '+B', 'unused': '*',
    'owner': '', 'owner_email': '',
    'channels': []
 }